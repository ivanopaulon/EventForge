@*
    Health Status Dialog — EFDialog + vertical MudTabs
    Replaced MudAppBar+MudGrid layout with EFDialog wrapper and tabbed interface.
*@

@using EventForge.DTOs.Health
@using EventForge.DTOs.SuperAdmin
@using EventForge.DTOs.Common
@using EventForge.DTOs.Profile
@using EventForge.DTOs.Auth
@using EventForge.Client.Services
@using EventForge.Client.Services.Updates
@using System.Timers
@inject ITranslationService TranslationService
@inject ILogManagementService LogManagementService
@inject IProfileService ProfileService
@inject IDialogService DialogService
@inject IAppNotificationService AppNotification
@inject IHealthService HealthService
@inject AuthenticationStateProvider AuthenticationStateProvider
@inject ILogger<HealthStatusDialog> Logger
@implements IDisposable

<EFDialog Icon="@Icons.Material.Outlined.MonitorHeart"
          Title="@TranslationService.GetTranslation("health.dialog.title", "System Health & Logs")"
          IsProcessing="@_isLoadingLogs"
          MinWidth="min(1000px, 95vw)"
          MinHeight="@EFDialogDefaults.MinHeightTabbed"
          MaxHeight="calc(100vh - 48px)"
          OnClose="Close">
    <ChildContent>
        <MudTabs Position="Position.Left"
                 Rounded="true"
                 Border="true"
                 MinimumTabWidth="140px"
                 Class="health-tabs">

            <!-- Tab 1: Status -->
            <MudTabPanel Text="@TranslationService.GetTranslation("health.tab.status", "Status")"
                         Icon="@Icons.Material.Outlined.MonitorHeart">
                @if (HealthData != null)
                {
                    <MudStack Spacing="3" Class="pa-3">

                        <!-- API Status Card -->
                        <MudCard Elevation="1">
                            <MudCardContent>
                                <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="2" Class="mb-3">
                                    <MudIcon Icon="@Icons.Material.Filled.Api"
                                             Color="@GetStatusColor(HealthData.ApiStatus)"
                                             Size="Size.Medium" />
                                    <MudText Typo="Typo.subtitle1" Class="font-weight-medium">
                                        @TranslationService.GetTranslation("health.api.title", "API Status")
                                    </MudText>
                                    <MudChip T="string" Size="Size.Small"
                                             Color="@GetStatusColor(HealthData.ApiStatus)"
                                             Text="@HealthData.ApiStatus"
                                             Variant="Variant.Filled" />
                                </MudStack>
                                <MudGrid Spacing="1">
                                    <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("health.version", "Version")</strong></MudText></MudItem>
                                    <MudItem xs="7"><MudText Typo="Typo.body2">@HealthData.Version</MudText></MudItem>
                                    <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("health.environment", "Environment")</strong></MudText></MudItem>
                                    <MudItem xs="7"><MudText Typo="Typo.body2">@HealthData.Environment</MudText></MudItem>
                                    <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("health.uptime", "Uptime")</strong></MudText></MudItem>
                                    <MudItem xs="7"><MudText Typo="Typo.body2">@FormatUptime(HealthData.Uptime)</MudText></MudItem>
                                </MudGrid>
                            </MudCardContent>
                        </MudCard>

                        <!-- Database Status Card -->
                        <MudCard Elevation="1">
                            <MudCardContent>
                                <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="2" Class="mb-3">
                                    <MudIcon Icon="@Icons.Material.Filled.Storage"
                                             Color="@GetStatusColor(HealthData.DatabaseStatus)"
                                             Size="Size.Medium" />
                                    <MudText Typo="Typo.subtitle1" Class="font-weight-medium">
                                        @TranslationService.GetTranslation("health.database.title", "Database Status")
                                    </MudText>
                                    <MudChip T="string" Size="Size.Small"
                                             Color="@GetStatusColor(HealthData.DatabaseStatus)"
                                             Text="@HealthData.DatabaseStatus"
                                             Variant="Variant.Filled" />
                                </MudStack>
                                <MudGrid Spacing="1">
                                    <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("health.status", "Status")</strong></MudText></MudItem>
                                    <MudItem xs="7"><MudText Typo="Typo.body2">@HealthData.DatabaseStatus</MudText></MudItem>
                                    @if (HealthData.DatabaseDetails?.Any() == true)
                                    {
                                        @foreach (var detail in HealthData.DatabaseDetails)
                                        {
                                            <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@detail.Key</strong></MudText></MudItem>
                                            <MudItem xs="7"><MudText Typo="Typo.body2">@detail.Value</MudText></MudItem>
                                        }
                                    }
                                    @if (HealthData.AppliedMigrations?.Any() == true)
                                    {
                                        <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("health.migrations", "Migrations")</strong></MudText></MudItem>
                                        <MudItem xs="7"><MudText Typo="Typo.body2">@HealthData.AppliedMigrations.Count()</MudText></MudItem>
                                    }
                                </MudGrid>
                            </MudCardContent>
                        </MudCard>

                        <!-- Last Updated -->
                        @if (LastUpdated.HasValue)
                        {
                            <MudAlert Severity="Severity.Info" NoIcon="false">
                                <MudText Typo="Typo.body2">
                                    <MudIcon Icon="@Icons.Material.Filled.Schedule" Size="Size.Small" Class="mr-1" />
                                    @TranslationService.GetTranslation("health.lastUpdated", "Last Updated"): @LastUpdated.Value.ToString("HH:mm:ss")
                                </MudText>
                            </MudAlert>
                        }

                        <!-- Current Session Card -->
                        @if (CurrentUser != null)
                        {
                            <MudCard Elevation="1">
                                <MudCardContent>
                                    <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="2" Class="mb-3">
                                        <MudIcon Icon="@Icons.Material.Filled.AccountCircle"
                                                 Color="Color.Primary"
                                                 Size="Size.Medium" />
                                        <MudText Typo="Typo.subtitle1" Class="font-weight-medium">
                                            @TranslationService.GetTranslation("health.currentSession", "Current Session")
                                        </MudText>
                                    </MudStack>
                                    <MudGrid Spacing="1">
                                        <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("field.username", "Username")</strong></MudText></MudItem>
                                        <MudItem xs="7"><MudText Typo="Typo.body2">@CurrentUser.Username</MudText></MudItem>
                                        <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("field.fullName", "Full Name")</strong></MudText></MudItem>
                                        <MudItem xs="7"><MudText Typo="Typo.body2">@CurrentUser.FullName</MudText></MudItem>
                                        <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("field.roles", "Roles")</strong></MudText></MudItem>
                                        <MudItem xs="7">
                                            @if (CurrentUser.Roles?.Any() == true)
                                            {
                                                <MudStack Row="true" Spacing="1">
                                                    @foreach (var role in CurrentUser.Roles)
                                                    {
                                                        <MudChip T="string" Size="Size.Small" Color="Color.Secondary" Text="@role" />
                                                    }
                                                </MudStack>
                                            }
                                            else
                                            {
                                                <MudText Typo="Typo.body2">-</MudText>
                                            }
                                        </MudItem>
                                        <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("field.permissions", "Permissions")</strong></MudText></MudItem>
                                        <MudItem xs="7">
                                            @if (CurrentUser.Permissions?.Any() == true)
                                            {
                                                <MudText Typo="Typo.body2">@CurrentUser.Permissions.Count @TranslationService.GetTranslation("health.permissionsGranted", "permissions granted")</MudText>
                                            }
                                            else
                                            {
                                                <MudText Typo="Typo.body2">-</MudText>
                                            }
                                        </MudItem>
                                        @if (CurrentUser.LastLoginAt.HasValue)
                                        {
                                            <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("field.lastLogin", "Last Login")</strong></MudText></MudItem>
                                            <MudItem xs="7"><MudText Typo="Typo.body2">@CurrentUser.LastLoginAt.Value.ToString("yyyy-MM-dd HH:mm:ss") UTC</MudText></MudItem>
                                        }
                                        @if (TokenExpiresAt.HasValue)
                                        {
                                            <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("health.tokenExpiry", "Token Expiry")</strong></MudText></MudItem>
                                            <MudItem xs="7"><MudText Typo="Typo.body2">@TokenExpiresAt.Value.ToString("yyyy-MM-dd HH:mm:ss") UTC</MudText></MudItem>
                                            <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("health.tokenTimeRemaining", "Time Remaining")</strong></MudText></MudItem>
                                            <MudItem xs="7"><MudText Typo="Typo.body2">@FormatTimeRemaining(TokenExpiresAt.Value)</MudText></MudItem>
                                        }
                                    </MudGrid>
                                </MudCardContent>
                            </MudCard>
                        }

                    </MudStack>
                }
                else
                {
                    <div class="text-center pa-4">
                        @if (!string.IsNullOrEmpty(ErrorMessage) && HealthData == null)
                        {
                            <MudAlert Severity="Severity.Error" Class="mb-4">
                                <MudText Typo="Typo.h6">@TranslationService.GetTranslation("health.error", "Health Check Error")</MudText>
                                <MudText Typo="Typo.body2">@ErrorMessage</MudText>
                            </MudAlert>
                            <MudButton Variant="Variant.Filled"
                                       Color="Color.Primary"
                                       OnClick="@RefreshAll"
                                       StartIcon="@Icons.Material.Filled.Refresh">
                                @TranslationService.GetTranslation("common.retry", "Retry")
                            </MudButton>
                        }
                        else
                        {
                            <MudProgressCircular Color="Color.Primary" Indeterminate="true" Class="mb-3" />
                            <MudText Typo="Typo.body1">
                                @TranslationService.GetTranslation("health.loading", "Loading health information...")
                            </MudText>
                        }
                    </div>
                }
            </MudTabPanel>

            <!-- Tab 2: UpdateAgent -->
            <MudTabPanel Text="Agent"
                         Icon="@Icons.Material.Outlined.Router">
                <MudStack Spacing="3" Class="pa-3">

                    <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="2">
                        <MudText Typo="Typo.subtitle1">UpdateAgent</MudText>
                        <MudIconButton Icon="@Icons.Material.Filled.Refresh"
                                       Size="Size.Small"
                                       OnClick="RefreshAgentStatusAsync"
                                       Disabled="@_isLoadingAgent"
                                       Title="Ricarica stato Agent" />
                    </MudStack>

                    @if (_isLoadingAgent)
                    {
                        <div class="text-center pa-4">
                            <MudProgressCircular Color="Color.Primary" Indeterminate="true" />
                        </div>
                    }
                    else if (_agentStatus is null)
                    {
                        <MudAlert Severity="Severity.Warning">
                            Agent non configurato o non raggiungibile. Verificare <code>Agent:LocalUrl</code> in appsettings.json.
                        </MudAlert>
                    }
                    else
                    {
                        @if (!_agentStatus.Reachable)
                        {
                            var unreachableMin = _agentStatus.UnreachableSinceUtc.HasValue
                                ? (int)(DateTime.UtcNow - _agentStatus.UnreachableSinceUtc.Value).TotalMinutes
                                : 0;
                            var autoRestartMin = _agentStatus.AutoRestartAfterMinutes;
                            var minutesToRestart = autoRestartMin > 0
                                ? Math.Max(0, autoRestartMin - unreachableMin)
                                : -1;

                            <MudAlert Severity="Severity.Warning" Icon="@Icons.Material.Filled.Warning">
                                <MudStack Spacing="1">
                                    <MudStack Row="true" AlignItems="AlignItems.Center" Justify="Justify.SpaceBetween" Class="flex-wrap" Style="gap:8px">
                                        <MudStack Spacing="0">
                                            <MudText Typo="Typo.body2">
                                                <strong>L'Agent non è raggiungibile</strong>
                                                @if (unreachableMin > 0)
                                                {
                                                    <span> da <strong>@unreachableMin min</strong></span>
                                                }
                                            </MudText>
                                            @if (autoRestartMin > 0)
                                            {
                                                @if (minutesToRestart <= 0)
                                                {
                                                    <MudText Typo="Typo.caption">
                                                        ⏳ Riavvio automatico in corso o imminente…
                                                    </MudText>
                                                }
                                                else
                                                {
                                                    <MudText Typo="Typo.caption">
                                                        ⏱ Riavvio automatico tra circa <strong>@minutesToRestart min</strong>
                                                        (soglia: @autoRestartMin min)
                                                    </MudText>
                                                }
                                            }
                                            else
                                            {
                                                <MudText Typo="Typo.caption">
                                                    Riavvio automatico disabilitato (<code>Agent:AutoRestartAfterMinutes = 0</code>)
                                                </MudText>
                                            }
                                        </MudStack>
                                        @if (_isAdmin)
                                        {
                                            <MudButton Variant="Variant.Filled"
                                                       Color="Color.Warning"
                                                       Size="Size.Small"
                                                       StartIcon="@Icons.Material.Filled.RestartAlt"
                                                       Disabled="@_isRestartingAgent"
                                                       OnClick="RestartAgentAsync">
                                                @(_isRestartingAgent ? "Riavvio in corso…" : "Riavvia ora")
                                            </MudButton>
                                        }
                                    </MudStack>
                                </MudStack>
                            </MudAlert>
                        }
                        <MudCard Elevation="1">
                            <MudCardContent>
                                <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="2" Class="mb-3">
                                    <MudIcon Icon="@Icons.Material.Filled.Router"
                                             Color="@(_agentStatus.Reachable ? Color.Success : Color.Error)"
                                             Size="Size.Medium" />
                                    <MudText Typo="Typo.subtitle1" Class="font-weight-medium">Stato Agent</MudText>
                                    <MudChip T="string" Size="Size.Small"
                                             Color="@(_agentStatus.Reachable ? Color.Success : Color.Error)"
                                             Text="@_agentStatus.Status"
                                             Variant="Variant.Filled" />
                                </MudStack>
                                <MudGrid Spacing="1">
                                    <MudItem xs="5"><MudText Typo="Typo.body2"><strong>Installazione</strong></MudText></MudItem>
                                    <MudItem xs="7"><MudText Typo="Typo.body2">@(_agentStatus.InstallationName ?? "—")</MudText></MudItem>

                                    <MudItem xs="5"><MudText Typo="Typo.body2"><strong>Versione Agent</strong></MudText></MudItem>
                                    <MudItem xs="7"><MudText Typo="Typo.body2">@(_agentStatus.AgentVersion ?? "—")</MudText></MudItem>

                                    <MudItem xs="5"><MudText Typo="Typo.body2"><strong>Versione Server</strong></MudText></MudItem>
                                    <MudItem xs="7"><MudText Typo="Typo.body2">@(_agentStatus.ServerVersion ?? "—")</MudText></MudItem>

                                    <MudItem xs="5"><MudText Typo="Typo.body2"><strong>Versione Client</strong></MudText></MudItem>
                                    <MudItem xs="7"><MudText Typo="Typo.body2">@(_agentStatus.ClientVersion ?? "—")</MudText></MudItem>

                                    <MudItem xs="5"><MudText Typo="Typo.body2"><strong>Hub Connection</strong></MudText></MudItem>
                                    <MudItem xs="7">
                                        <MudChip T="string" Size="Size.Small"
                                                 Color="@(_agentStatus.HubConnectionState == "Connected" ? Color.Success : Color.Warning)"
                                                 Text="@(_agentStatus.HubConnectionState ?? "Unknown")" />
                                    </MudItem>

                                    @if (_agentStatus.LastHeartbeatAt.HasValue)
                                    {
                                        <MudItem xs="5"><MudText Typo="Typo.body2"><strong>Ultimo heartbeat</strong></MudText></MudItem>
                                        <MudItem xs="7"><MudText Typo="Typo.body2">@_agentStatus.LastHeartbeatAt.Value.ToLocalTime().ToString("HH:mm:ss dd/MM/yyyy")</MudText></MudItem>
                                    }

                                    <MudItem xs="5"><MudText Typo="Typo.body2"><strong>Sondato alle</strong></MudText></MudItem>
                                    <MudItem xs="7"><MudText Typo="Typo.body2">@_agentStatus.ProbedAt.ToLocalTime().ToString("HH:mm:ss")</MudText></MudItem>
                                </MudGrid>
                            </MudCardContent>
                        </MudCard>
                    }

                </MudStack>
            </MudTabPanel>

            <!-- Tab 3: Sessioni attive -->
            <MudTabPanel Text="@TranslationService.GetTranslation("health.tab.sessions", "Sessions")"
                         Icon="@Icons.Material.Outlined.Devices">
                <div class="pa-3">
                    @if (ActiveSessions != null && ActiveSessions.Any())
                    {
                        <MudStack Spacing="2">
                            <MudPaper Elevation="1" Class="pa-3">
                                <MudStack Row="true" Justify="Justify.SpaceBetween" AlignItems="AlignItems.Center">
                                    <MudText Typo="Typo.body2">
                                        @ActiveSessions.Count @TranslationService.GetTranslation("health.sessionsActive", "active session(s)")
                                    </MudText>
                                    <MudButton Variant="Variant.Outlined"
                                               Color="Color.Warning"
                                               Size="Size.Small"
                                               StartIcon="@Icons.Material.Filled.PowerOff"
                                               OnClick="@TerminateAllOtherSessions"
                                               Disabled="@_isTerminatingSession">
                                        @TranslationService.GetTranslation("health.terminateOtherSessions", "Terminate Other Sessions")
                                    </MudButton>
                                </MudStack>
                            </MudPaper>

                            <MudTable Items="@ActiveSessions"
                                      Hover="true"
                                      Striped="true"
                                      Dense="true"
                                      Elevation="1">
                                <HeaderContent>
                                    <MudTh>@TranslationService.GetTranslation("health.session", "Session")</MudTh>
                                    <MudTh>@TranslationService.GetTranslation("health.device", "Device")</MudTh>
                                    <MudTh>@TranslationService.GetTranslation("health.location", "Location")</MudTh>
                                    <MudTh>@TranslationService.GetTranslation("health.lastActivity", "Last Activity")</MudTh>
                                    <MudTh>@TranslationService.GetTranslation("health.actions", "Actions")</MudTh>
                                </HeaderContent>
                                <RowTemplate>
                                    <MudTd DataLabel="Session">
                                        <MudStack Spacing="0">
                                            @if (context.IsCurrentSession)
                                            {
                                                <MudChip T="string" Size="Size.Small" Color="Color.Success" Text="@TranslationService.GetTranslation("health.currentSession", "Current")" />
                                            }
                                            <MudText Typo="Typo.caption">@context.IpAddress</MudText>
                                        </MudStack>
                                    </MudTd>
                                    <MudTd DataLabel="Device">
                                        <MudStack Spacing="0">
                                            <MudText Typo="Typo.body2">@GetDeviceIcon(context.DeviceType) @context.Browser</MudText>
                                            <MudText Typo="Typo.caption">@context.OperatingSystem</MudText>
                                        </MudStack>
                                    </MudTd>
                                    <MudTd DataLabel="Location">
                                        <MudText Typo="Typo.body2">@(context.Location ?? "-")</MudText>
                                    </MudTd>
                                    <MudTd DataLabel="Last Activity">
                                        <MudStack Spacing="0">
                                            <MudText Typo="Typo.body2">@context.LastActivity.ToString("yyyy-MM-dd")</MudText>
                                            <MudText Typo="Typo.caption">@context.LastActivity.ToString("HH:mm:ss") UTC</MudText>
                                        </MudStack>
                                    </MudTd>
                                    <MudTd DataLabel="Actions">
                                        <MudStack Row="true" Spacing="1">
                                            <MudIconButton Icon="@Icons.Material.Outlined.Visibility"
                                                           Size="Size.Small"
                                                           Color="Color.Info"
                                                           OnClick="@(() => ShowSessionDetails(context))"
                                                           Title="@TranslationService.GetTranslation("health.viewDetails", "View Details")" />
                                            @if (!context.IsCurrentSession)
                                            {
                                                <MudIconButton Icon="@Icons.Material.Filled.PowerOff"
                                                               Size="Size.Small"
                                                               Color="Color.Error"
                                                               OnClick="@(() => TerminateSession(context.Id))"
                                                               Disabled="@_isTerminatingSession"
                                                               Title="@TranslationService.GetTranslation("health.terminateSession", "Terminate")" />
                                            }
                                        </MudStack>
                                    </MudTd>
                                </RowTemplate>
                            </MudTable>
                        </MudStack>

                        <!-- Session Details Panel -->
                        @if (_selectedSessionForDetails != null)
                        {
                            <MudPaper Elevation="2" Class="mt-4 pa-4">
                                <div class="d-flex justify-space-between align-center mb-3">
                                    <MudText Typo="Typo.h6">
                                        @TranslationService.GetTranslation("health.sessionDetails", "Session Details")
                                    </MudText>
                                    <MudIconButton Icon="@Icons.Material.Filled.Close"
                                                   Size="Size.Small"
                                                   OnClick="@(() => _selectedSessionForDetails = null)" />
                                </div>
                                <MudGrid Spacing="1">
                                    <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("health.sessionId", "Session ID")</strong></MudText></MudItem>
                                    <MudItem xs="7"><MudText Typo="Typo.body2"><code>@_selectedSessionForDetails.Id</code></MudText></MudItem>
                                    <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("health.ipAddress", "IP Address")</strong></MudText></MudItem>
                                    <MudItem xs="7"><MudText Typo="Typo.body2">@_selectedSessionForDetails.IpAddress</MudText></MudItem>
                                    <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("health.browser", "Browser")</strong></MudText></MudItem>
                                    <MudItem xs="7"><MudText Typo="Typo.body2">@_selectedSessionForDetails.Browser</MudText></MudItem>
                                    <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("health.operatingSystem", "Operating System")</strong></MudText></MudItem>
                                    <MudItem xs="7"><MudText Typo="Typo.body2">@_selectedSessionForDetails.OperatingSystem</MudText></MudItem>
                                    <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("health.deviceType", "Device Type")</strong></MudText></MudItem>
                                    <MudItem xs="7"><MudText Typo="Typo.body2">@_selectedSessionForDetails.DeviceType</MudText></MudItem>
                                    <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("health.loginTime", "Login Time")</strong></MudText></MudItem>
                                    <MudItem xs="7"><MudText Typo="Typo.body2">@_selectedSessionForDetails.LoginTime.ToString("yyyy-MM-dd HH:mm:ss") UTC</MudText></MudItem>
                                    <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("health.lastActivity", "Last Activity")</strong></MudText></MudItem>
                                    <MudItem xs="7"><MudText Typo="Typo.body2">@_selectedSessionForDetails.LastActivity.ToString("yyyy-MM-dd HH:mm:ss") UTC</MudText></MudItem>
                                    <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("health.location", "Location")</strong></MudText></MudItem>
                                    <MudItem xs="7"><MudText Typo="Typo.body2">@(_selectedSessionForDetails.Location ?? "-")</MudText></MudItem>
                                    <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("health.userAgent", "User Agent")</strong></MudText></MudItem>
                                    <MudItem xs="7"><MudText Typo="Typo.body2" Style="word-break:break-all;">@_selectedSessionForDetails.UserAgent</MudText></MudItem>
                                    <MudItem xs="5"><MudText Typo="Typo.body2"><strong>@TranslationService.GetTranslation("health.isCurrentSession", "Is Current Session")</strong></MudText></MudItem>
                                    <MudItem xs="7"><MudText Typo="Typo.body2">@(_selectedSessionForDetails.IsCurrentSession ? TranslationService.GetTranslation("common.yes", "Yes") : TranslationService.GetTranslation("common.no", "No"))</MudText></MudItem>
                                </MudGrid>
                            </MudPaper>
                        }
                    }
                    else if (ActiveSessions == null)
                    {
                        <MudAlert Severity="Severity.Warning" NoIcon="false" Class="ma-3">
                            @TranslationService.GetTranslation("health.sessionsNotAvailable", "Session information is not available")
                        </MudAlert>
                    }
                    else
                    {
                        <MudAlert Severity="Severity.Info" NoIcon="false" Class="ma-3">
                            @TranslationService.GetTranslation("health.noActiveSessions", "No active sessions")
                        </MudAlert>
                    }
                </div>
            </MudTabPanel>

            <!-- Tab 3: Log di sistema -->
            <MudTabPanel Text="@TranslationService.GetTranslation("health.tab.logs", "Logs")"
                         Icon="@Icons.Material.Outlined.Article">
                <div class="pa-3">
                    <!-- Auto-refresh controls -->
                    <MudPaper Elevation="1" Class="pa-3 mb-3">
                        <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="2">
                            <MudSwitch @bind-Value="_autoRefreshEnabled"
                                       Color="Color.Primary"
                                       T="bool"
                                       Label="@TranslationService.GetTranslation("health.autoRefresh", "Auto-refresh")"
                                       aria-label="@TranslationService.GetTranslation("health.autoRefresh", "Auto-refresh")" />
                            @if (_autoRefreshEnabled)
                            {
                                <MudNumericField @bind-Value="_refreshIntervalSeconds"
                                                 Label="@TranslationService.GetTranslation("health.intervalSeconds", "Interval (s)")"
                                                 Min="5"
                                                 Max="300"
                                                 Step="5"
                                                 Style="width: 120px;"
                                                 Variant="Variant.Outlined"
                                                 Margin="Margin.Dense" />
                            }
                        </MudStack>
                    </MudPaper>

                    <!-- Filters -->
                    <MudExpansionPanels Elevation="0" Class="mb-3">
                        <MudExpansionPanel Text="@TranslationService.GetTranslation("health.logFilters", "Log Filters")"
                                           Icon="@Icons.Material.Outlined.FilterList"
                                           Dense="true"
                                           @bind-IsExpanded="_filtersExpanded">
                            <MudGrid Spacing="2">
                                <MudItem xs="12" md="3">
                                    <MudSelect T="string" @bind-Value="_selectedLogLevel" @bind-Value:after="OnFilterChanged"
                                               Label="@TranslationService.GetTranslation("health.logLevel", "Level")"
                                               Variant="Variant.Outlined"
                                               Clearable="true"
                                               Dense="true">
                                        <MudSelectItem Value="@("")">@TranslationService.GetTranslation("common.all", "All")</MudSelectItem>
                                        <MudSelectItem Value="@("Debug")">Debug</MudSelectItem>
                                        <MudSelectItem Value="@("Information")">Information</MudSelectItem>
                                        <MudSelectItem Value="@("Warning")">Warning</MudSelectItem>
                                        <MudSelectItem Value="@("Error")">Error</MudSelectItem>
                                        <MudSelectItem Value="@("Critical")">Critical</MudSelectItem>
                                    </MudSelect>
                                </MudItem>
                                <MudItem xs="12" md="3">
                                    <MudDatePicker @bind-Date="_startDate" @bind-Date:after="OnFilterChanged"
                                                   Label="@TranslationService.GetTranslation("health.startDate", "From Date")"
                                                   Variant="Variant.Outlined"
                                                   Dense="true"
                    Margin="Margin.Dense"/>
                                </MudItem>
                                <MudItem xs="12" md="3">
                                    <MudDatePicker @bind-Date="_endDate" @bind-Date:after="OnFilterChanged"
                                                   Label="@TranslationService.GetTranslation("health.endDate", "To Date")"
                                                   Variant="Variant.Outlined"
                                                   Dense="true"
                    Margin="Margin.Dense"/>
                                </MudItem>
                                <MudItem xs="12" md="3">
                                    <MudTextField @bind-Value="_searchText" @bind-Value:after="OnFilterChanged"
                                                  Label="@TranslationService.GetTranslation("health.searchMessage", "Search message")"
                                                  Placeholder="@TranslationService.GetTranslation("health.searchPlaceholder", "Search...")"
                                                  Variant="Variant.Outlined"
                                                  Clearable="true"
                                                  Dense="true"
                    Margin="Margin.Dense"/>
                                </MudItem>
                            </MudGrid>
                            <div class="d-flex justify-end mt-3">
                                <MudButton Variant="Variant.Text"
                                           Color="Color.Primary"
                                           StartIcon="@Icons.Material.Outlined.Clear"
                                           OnClick="@ClearFilters"
                                           Size="Size.Small">
                                    @TranslationService.GetTranslation("health.clearFilters", "Clear filters")
                                </MudButton>
                            </div>
                        </MudExpansionPanel>
                    </MudExpansionPanels>

                    <!-- Logs Table -->
                    <MudPaper Elevation="1">
                        @if (_isLoadingLogs)
                        {
                            <div class="text-center pa-4">
                                <MudProgressCircular Color="Color.Primary" Indeterminate="true" />
                            </div>
                        }
                        else if (_isAdmin && _logs?.Items?.Any() == true)
                        {
                            <MudTable Items="@_logs.Items"
                                      Hover="true"
                                      Striped="true"
                                      Dense="true"
                                      Elevation="0">
                                <HeaderContent>
                                    <MudTh>@TranslationService.GetTranslation("health.timestamp", "Timestamp")</MudTh>
                                    <MudTh>@TranslationService.GetTranslation("health.level", "Level")</MudTh>
                                    <MudTh>@TranslationService.GetTranslation("health.message", "Message")</MudTh>
                                    <MudTh>@TranslationService.GetTranslation("health.actions", "Actions")</MudTh>
                                </HeaderContent>
                                <RowTemplate>
                                    <MudTd DataLabel="Timestamp">@context.Timestamp.ToString("yyyy-MM-dd HH:mm:ss")</MudTd>
                                    <MudTd DataLabel="Level">
                                        <MudChip T="string"
                                                 Size="Size.Small"
                                                 Color="@GetLogLevelColor(context.Level)"
                                                 Text="@context.Level" />
                                    </MudTd>
                                    <MudTd DataLabel="Message">
                                        <MudText Typo="Typo.body2" Style="max-width: 300px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;">
                                            @context.Message
                                        </MudText>
                                    </MudTd>
                                    <MudTd DataLabel="Actions">
                                        <MudIconButton Icon="@Icons.Material.Outlined.Visibility"
                                                       Size="Size.Small"
                                                       Color="Color.Info"
                                                       OnClick="@(() => ShowLogDetails(context))" />
                                    </MudTd>
                                </RowTemplate>
                            </MudTable>
                            <div class="d-flex justify-center pa-3">
                                <MudPagination Count="@_logs.TotalPages"
                                               Selected="@_currentPage"
                                               SelectedChanged="@OnPageChanged"
                                               Color="Color.Primary" />
                            </div>
                        }
                        else if (!_isAdmin && _sanitizedLogs?.Items?.Any() == true)
                        {
                            <MudTable Items="@_sanitizedLogs.Items"
                                      Hover="true"
                                      Striped="true"
                                      Dense="true"
                                      Elevation="0">
                                <HeaderContent>
                                    <MudTh>@TranslationService.GetTranslation("health.timestamp", "Timestamp")</MudTh>
                                    <MudTh>@TranslationService.GetTranslation("health.level", "Level")</MudTh>
                                    <MudTh>@TranslationService.GetTranslation("health.message", "Message")</MudTh>
                                    <MudTh>@TranslationService.GetTranslation("health.actions", "Actions")</MudTh>
                                </HeaderContent>
                                <RowTemplate>
                                    <MudTd DataLabel="Timestamp">@context.Timestamp.ToString("yyyy-MM-dd HH:mm:ss")</MudTd>
                                    <MudTd DataLabel="Level">
                                        <MudChip T="string"
                                                 Size="Size.Small"
                                                 Color="@GetLogLevelColor(context.Level)"
                                                 Text="@context.Level" />
                                    </MudTd>
                                    <MudTd DataLabel="Message">
                                        <MudText Typo="Typo.body2" Style="max-width: 300px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;">
                                            @context.Message
                                        </MudText>
                                    </MudTd>
                                    <MudTd DataLabel="Actions">
                                        <MudIconButton Icon="@Icons.Material.Outlined.Visibility"
                                                       Size="Size.Small"
                                                       Color="Color.Info"
                                                       OnClick="@(() => ShowSanitizedLogDetails(context))" />
                                    </MudTd>
                                </RowTemplate>
                            </MudTable>
                            <div class="d-flex justify-center pa-3">
                                <MudPagination Count="@_sanitizedLogs.TotalPages"
                                               Selected="@_currentPage"
                                               SelectedChanged="@OnPageChanged"
                                               Color="Color.Primary" />
                            </div>
                        }
                        else
                        {
                            <div class="text-center pa-4">
                                <MudText Typo="Typo.body1" Color="Color.Secondary">
                                    @TranslationService.GetTranslation("health.noLogs", "No logs found")
                                </MudText>
                            </div>
                        }
                    </MudPaper>

                </div>
            </MudTabPanel>

        </MudTabs>
    </ChildContent>
    <DialogActions>
        <MudIconButton Icon="@Icons.Material.Outlined.Refresh" OnClick="RefreshAll" Title="@TranslationService.GetTranslation("common.refresh", "Refresh")" />
        <MudIconButton Icon="@Icons.Material.Outlined.FileDownload" OnClick="ExportLogs" Title="@TranslationService.GetTranslation("health.export", "Export")" />
        <MudButton Variant="Variant.Filled" Color="Color.Primary" StartIcon="@Icons.Material.Outlined.Close" OnClick="Close">
            @TranslationService.GetTranslation("button.close", "Close")
        </MudButton>
    </DialogActions>
</EFDialog>

<style>
    .health-tabs .mud-tabs-panels {
        overflow-y: auto;
        flex: 1 1 auto;
        min-height: 0;
    }
</style>

@code {
    [CascadingParameter] IMudDialogInstance MudDialog { get; set; } = default!;
    
    [Parameter] public DetailedHealthStatusDto? HealthData { get; set; }
    [Parameter] public DateTime? LastUpdated { get; set; }
    [Parameter] public EventCallback OnRefreshRequested { get; set; }
    [Parameter] public List<ActiveSessionDto>? ActiveSessions { get; set; }
    [Parameter] public UserDto? CurrentUser { get; set; }
    [Parameter] public DateTime? TokenExpiresAt { get; set; }
    [Parameter] public string? ErrorMessage { get; set; }

    // Agent Status State
    private AgentStatusClientDto? _agentStatus;
    private bool _isLoadingAgent = false;
    private bool _isRestartingAgent = false;

    // Log Management State
    private bool _filtersExpanded = false;
    private bool _isLoadingLogs = false;
    private PagedResult<SystemLogDto>? _logs;
    private PagedResult<SanitizedSystemLogDto>? _sanitizedLogs;
    private int _currentPage = 1;
    private int _pageSize = 20;
    private bool _isAdmin = false;

    // Filter State
    private string _selectedLogLevel = string.Empty;
    private DateTime? _startDate;
    private DateTime? _endDate;
    private string _searchText = string.Empty;

    // Auto-refresh State
    private bool _autoRefreshEnabled = false;
    private int _refreshIntervalSeconds = 30;
    private Timer? _refreshTimer;

    // Session Management State
    private bool _isTerminatingSession = false;
    private ActiveSessionDto? _selectedSessionForDetails;

    protected override async Task OnInitializedAsync()
    {
        try
        {
            // Set default date range to last 24 hours
            _endDate = DateTime.Now;
            _startDate = DateTime.Now.AddDays(-1);
            
            // Check if user is admin
            await DetermineUserRole();

            // Load agent status and initial logs in parallel
            await Task.WhenAll(RefreshAgentStatusAsync(), LoadLogsAsync());
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Error in OnInitializedAsync.");
            AppNotification.ShowWarning("Si è verificato un errore imprevisto.");
        }
    }

    protected override void OnParametersSet()
    {
        // Update auto-refresh when enabled status changes
        if (_autoRefreshEnabled)
        {
            StartAutoRefresh();
        }
        else
        {
            StopAutoRefresh();
        }
    }

    private async Task RefreshAgentStatusAsync()
    {
        _isLoadingAgent = true;
        try
        {
            _agentStatus = await HealthService.GetAgentStatusAsync();
        }
        catch
        {
            _agentStatus = null;
        }
        finally
        {
            _isLoadingAgent = false;
            StateHasChanged();
        }
    }

    private async Task RestartAgentAsync()
    {
        _isRestartingAgent = true;
        StateHasChanged();
        try
        {
            var (success, message) = await HealthService.RestartAgentAsync();
            if (success)
                AppNotification.ShowSuccess(message);
            else
                AppNotification.ShowWarning(message);

            // Re-probe after a short delay to pick up new state
            await Task.Delay(3000);
            await RefreshAgentStatusAsync();
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Error in RestartAgentAsync.");
            AppNotification.ShowWarning("Si è verificato un errore imprevisto.");
        }
        finally
        {
            _isRestartingAgent = false;
            StateHasChanged();
        }
    }

    private async Task DetermineUserRole()
    {
        try
        {
            var authState = await AuthenticationStateProvider.GetAuthenticationStateAsync();
            var user = authState.User;
            _isAdmin = user.IsInRole("SuperAdmin") || user.IsInRole("Admin");
        }
        catch (Exception ex)
        {
            _isAdmin = false;
            Logger.LogError(ex, "Error in DetermineUserRole.");
        }
    }

    private void StartAutoRefresh()
    {
        StopAutoRefresh(); // Stop any existing timer
        
        _refreshTimer = new Timer(_refreshIntervalSeconds * 1000);
        _refreshTimer.Elapsed += async (sender, e) => await OnAutoRefreshTick();
        _refreshTimer.AutoReset = true;
        _refreshTimer.Start();
    }

    private void StopAutoRefresh()
    {
        if (_refreshTimer != null)
        {
            _refreshTimer.Stop();
            _refreshTimer.Dispose();
            _refreshTimer = null;
        }
    }

    private async Task OnAutoRefreshTick()
    {
        try
        {
            await InvokeAsync(async () =>
            {
                await RefreshAll();
                StateHasChanged();
            });
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Error in OnAutoRefreshTick.");
        }
    }

    private async Task RefreshAll()
    {
        // Refresh health data
        if (OnRefreshRequested.HasDelegate)
        {
            await OnRefreshRequested.InvokeAsync();
        }
        
        // Refresh logs
        await LoadLogsAsync();
        
        // Update last updated timestamp
        LastUpdated = DateTime.Now;
    }

    private void Close()
    {
        StopAutoRefresh();
        MudDialog?.Close();
    }

    private async Task LoadLogsAsync()
    {
        _isLoadingLogs = true;
        try
        {
            var queryParams = new ApplicationLogQueryParameters
            {
                Level = string.IsNullOrEmpty(_selectedLogLevel) ? null : _selectedLogLevel,
                FromDate = _startDate,
                ToDate = _endDate,
                Message = string.IsNullOrEmpty(_searchText) ? null : _searchText,
                Page = _currentPage,
                PageSize = _pageSize,
                SortBy = "Timestamp",
                SortDirection = "desc"
            };

            if (_isAdmin)
            {
                // Admin users get full logs
                _logs = await LogManagementService.GetApplicationLogsAsync(queryParams);
                _sanitizedLogs = null;
            }
            else
            {
                // Non-admin users get sanitized logs
                _sanitizedLogs = await LogManagementService.GetPublicApplicationLogsAsync(queryParams);
                _logs = null;
            }
        }
        catch (Exception ex)
        {
            AppNotification.ShowError(TranslationService.GetTranslation("health.errorLoadingLogs", "Error loading logs: {0}", ex.Message));
            
            if (_isAdmin)
            {
                _logs = new PagedResult<SystemLogDto>
                {
                    Items = new List<SystemLogDto>(),
                    Page = _currentPage,
                    PageSize = _pageSize,
                    TotalCount = 0
                };
            }
            else
            {
                _sanitizedLogs = new PagedResult<SanitizedSystemLogDto>
                {
                    Items = new List<SanitizedSystemLogDto>(),
                    Page = _currentPage,
                    PageSize = _pageSize,
                    TotalCount = 0
                };
            }
        }
        finally
        {
            _isLoadingLogs = false;
            StateHasChanged();
        }
    }

    private async Task OnFilterChanged()
    {
        _currentPage = 1;
        await LoadLogsAsync();
    }

    private void ClearFilters()
    {
        _selectedLogLevel = string.Empty;
        _startDate = DateTime.Now.AddDays(-1);
        _endDate = DateTime.Now;
        _searchText = string.Empty;
        _currentPage = 1;
        _ = LoadLogsAsync();
    }

    private async Task OnPageChanged(int page)
    {
        _currentPage = page;
        await LoadLogsAsync();
    }

    private async Task ShowLogDetails(SystemLogDto log)
    {
        Logger.LogDebug("Opening log detail dialog for entry {Id}", log.Id);
        var parameters = new DialogParameters { { nameof(LogDetailDialog.AdminLog), log } };
        await DialogService.ShowAsync<LogDetailDialog>(string.Empty, parameters, EFDialogDefaults.Options);
    }

    private async Task ShowSanitizedLogDetails(SanitizedSystemLogDto log)
    {
        Logger.LogDebug("Opening sanitized log detail dialog for entry {Id}", log.Id);
        var parameters = new DialogParameters { { nameof(LogDetailDialog.SanitizedLog), log } };
        await DialogService.ShowAsync<LogDetailDialog>(string.Empty, parameters, EFDialogDefaults.Options);
    }

    private async Task ExportLogs()
    {
        // Stub implementation for now
        AppNotification.ShowInfo(TranslationService.GetTranslation("health.exportNotImplemented", "Export feature coming soon"));
        await Task.CompletedTask;
    }

    private Color GetLogLevelColor(string level)
    {
        return level?.ToLower() switch
        {
            "debug" => Color.Info,
            "information" => Color.Primary,
            "warning" => Color.Warning,
            "error" => Color.Error,
            "critical" => Color.Error,
            _ => Color.Default
        };
    }

    private Color GetStatusColor(string? status)
    {
        if (string.IsNullOrEmpty(status)) return Color.Default;
        
        return status.ToLower() switch
        {
            "healthy" => Color.Success,
            "degraded" => Color.Warning,
            "unhealthy" or "error" => Color.Error,
            "disabled" => Color.Info,
            _ => Color.Default
        };
    }

    private Color GetOverallHealthColor()
    {
        if (HealthData == null) return Color.Default;

        var statuses = new List<string?>
        {
            HealthData.ApiStatus,
            HealthData.DatabaseStatus
        };

        if (statuses.Any(s => s?.ToLower() == "unhealthy" || s?.ToLower() == "error"))
            return Color.Error;
        else if (statuses.Any(s => s?.ToLower() == "degraded" || s?.ToLower() == "warning"))
            return Color.Warning;
        else if (statuses.Any(s => s?.ToLower() == "healthy"))
            return Color.Success;
        else
            return Color.Info;
    }

    private string GetOverallHealthIcon()
    {
        if (HealthData == null) return Icons.Material.Filled.Help;

        var overallStatus = GetOverallHealthStatus();
        return overallStatus.ToLower() switch
        {
            "healthy" => Icons.Material.Filled.CheckCircle,
            "degraded" => Icons.Material.Filled.Warning,
            "unhealthy" => Icons.Material.Filled.Error,
            _ => Icons.Material.Filled.Help
        };
    }

    private string GetOverallHealthStatus()
    {
        if (HealthData == null) return "Unknown";

        var statuses = new List<string?>
        {
            HealthData.ApiStatus,
            HealthData.DatabaseStatus
        };

        if (statuses.Any(s => s?.ToLower() == "unhealthy" || s?.ToLower() == "error"))
            return "Unhealthy";
        else if (statuses.Any(s => s?.ToLower() == "degraded" || s?.ToLower() == "warning"))
            return "Degraded";
        else if (statuses.Any(s => s?.ToLower() == "healthy"))
            return "Healthy";
        else
            return "Unknown";
    }

    private string FormatUptime(TimeSpan uptime)
    {
        if (uptime.TotalDays >= 1)
            return $"{uptime.Days}d {uptime.Hours}h {uptime.Minutes}m";
        else if (uptime.TotalHours >= 1)
            return $"{uptime.Hours}h {uptime.Minutes}m";
        else
            return $"{uptime.Minutes}m {uptime.Seconds}s";
    }

    private string FormatTimeRemaining(DateTime expiresAt)
    {
        var remaining = expiresAt - DateTime.UtcNow;
        if (remaining.TotalSeconds <= 0)
            return TranslationService.GetTranslation("health.expired", "Expired");
        
        if (remaining.TotalDays >= 1)
            return $"{remaining.Days}d {remaining.Hours}h {remaining.Minutes}m";
        else if (remaining.TotalHours >= 1)
            return $"{remaining.Hours}h {remaining.Minutes}m {remaining.Seconds}s";
        else if (remaining.TotalMinutes >= 1)
            return $"{remaining.Minutes}m {remaining.Seconds}s";
        else
            return $"{remaining.Seconds}s";
    }

    private string GetDeviceIcon(string? deviceType)
    {
        return deviceType?.ToLower() switch
        {
            "mobile" => "📱",
            "tablet" => "📲",
            "desktop" => "💻",
            _ => "🖥️"
        };
    }

    private void ShowSessionDetails(ActiveSessionDto session)
    {
        _selectedSessionForDetails = session;
        StateHasChanged();
    }

    private async Task TerminateSession(Guid sessionId)
    {
        if (_isTerminatingSession)
            return;

        try
        {
            _isTerminatingSession = true;
            StateHasChanged();

            var success = await ProfileService.TerminateSessionAsync(sessionId);
            
            if (success)
            {
                // Remove the terminated session from the list
                ActiveSessions?.RemoveAll(s => s.Id == sessionId);
                
                AppNotification.ShowSuccess(TranslationService.GetTranslation("health.sessionTerminated", "Session terminated successfully"));
            }
            else
            {
                AppNotification.ShowError(TranslationService.GetTranslation("health.sessionTerminationFailed", "Failed to terminate session"));
            }
        }
        catch (Exception ex)
        {
            AppNotification.ShowError(TranslationService.GetTranslation("health.errorTerminatingSession", "Error terminating session: {0}", ex.Message));
        }
        finally
        {
            _isTerminatingSession = false;
            StateHasChanged();
        }
    }

    private async Task TerminateAllOtherSessions()
    {
        if (_isTerminatingSession)
            return;

        // Ask for confirmation
        var confirmed = await DialogService.ShowMessageBoxAsync(
            TranslationService.GetTranslation("health.confirmTermination", "Confirm Termination"),
            TranslationService.GetTranslation("health.confirmTerminateAllOtherSessions", "Are you sure you want to terminate all other sessions? This action cannot be undone."),
            yesText: TranslationService.GetTranslation("common.yes", "Yes"),
            noText: TranslationService.GetTranslation("common.no", "No")
        );

        if (confirmed != true)
            return;

        try
        {
            _isTerminatingSession = true;
            StateHasChanged();

            var success = await ProfileService.TerminateAllOtherSessionsAsync();
            
            if (success)
            {
                // Remove all non-current sessions from the list
                ActiveSessions?.RemoveAll(s => !s.IsCurrentSession);
                
                AppNotification.ShowSuccess(TranslationService.GetTranslation("health.allOtherSessionsTerminated", "All other sessions terminated successfully"));
            }
            else
            {
                AppNotification.ShowError(TranslationService.GetTranslation("health.terminationFailed", "Failed to terminate other sessions"));
            }
        }
        catch (Exception ex)
        {
            AppNotification.ShowError(TranslationService.GetTranslation("health.errorTerminatingSessions", "Error terminating sessions: {0}", ex.Message));
        }
        finally
        {
            _isTerminatingSession = false;
            StateHasChanged();
        }
    }

    public void Dispose()
    {
        StopAutoRefresh();
    }
}