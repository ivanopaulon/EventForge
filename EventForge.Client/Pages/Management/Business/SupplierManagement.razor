@page "/business/suppliers"
@inject NavigationManager NavigationManager

<PageTitle>Fornitori</PageTitle>

@code {
    protected override void OnInitialized()
    {
        // Redirect to unified BusinessPartyManagement with suppliers filter
        NavigationManager.NavigateTo("/business/parties/suppliers", replace: true);
    }
}
