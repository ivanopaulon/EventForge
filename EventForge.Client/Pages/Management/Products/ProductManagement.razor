@page "/product-management/products"
@using Microsoft.AspNetCore.Authorization
@using EventForge.DTOs.Products
@using EventForge.DTOs.Common
@using EventForge.Client.Shared.Components
@using EventForge.Client.Shared.Components.Dashboard
@using System.Diagnostics
@attribute [Authorize]
@inject IAuthService AuthService
@inject IProductService ProductService
@inject NavigationManager NavigationManager
@inject IAuthenticationDialogService AuthenticationDialogService
@inject ISnackbar Snackbar
@inject IDialogService DialogService
@inject ITranslationService TranslationService
@inject ILogger<ProductManagement> Logger

<PageLoadingOverlay Visible="_isLoading || _isLoadingProducts"
                     Message="@(_isLoading ? TranslationService.GetTranslation("messages.loadingPage", "Caricamento pagina...") : TranslationService.GetTranslation("common.loading", "Caricamento..."))" />

@if (!_isLoading)
{
    <div class="product-page-root">
        <div class="product-top">
            <ManagementDashboard TItem="ProductDto"
                                 Items="_filteredProducts"
                                 Metrics="_dashboardMetrics"
                                 EntityType="Product"
                                 AllowConfiguration="true"
                                 UseServerSide="false" />
        </div>

        <div class="eftable-wrapper">
            <MudPaper Elevation="0" Class="mb-3">
                <MudToolBar>
                    <MudText Typo="Typo.h5">
                        @TranslationService.GetTranslation("product.management", "Gestione Prodotti")
                    </MudText>
                    <MudSpacer />
                    <MudTextField @bind-Value="_searchTerm"
                                  @bind-Value:after="OnSearchChanged"
                                  Label="@TranslationService.GetTranslation("product.search", "Cerca prodotti")"
                                  Placeholder="@TranslationService.GetTranslation("product.searchPlaceholder", "Inserisci nome...")"
                                  Variant="Variant.Outlined"
                                  Adornment="Adornment.End"
                                  AdornmentIcon="@Icons.Material.Outlined.Search"
                                  Clearable="true"
                                  Class="ef-input" />
                    <ManagementTableToolbar ShowSelectionBadge="false"
                                            SelectedCount="@_selectedProducts.Count"
                                            ShowRefresh="true"
                                            ShowCreate="true"
                                            ShowDelete="false"
                                            CreateTooltip="product.createNew"
                                            IsDisabled="_isLoadingProducts"
                                            OnRefresh="@LoadProductsAsync"
                                            OnCreate="@CreateProduct" />
                </MudToolBar>
            </MudPaper>

            @if (_isLoadingProducts)
            {
                <MudProgressLinear Color="Color.Primary" Indeterminate="true" Class="mb-2" />
            }

            @if (_filteredProducts.Any())
            {
                <VirtualizedEFTable Items="@_filteredProducts.ToList()" 
                                    ItemSize="60" 
                                    TItem="ProductDto">
                    <ItemContent Context="product">
                        <MudPaper Class="pa-2 mb-1" Elevation="1">
                            <MudGrid>
                                <MudItem xs="4">
                                    <MudText Typo="Typo.body2"><b>@product.Code</b></MudText>
                                    <MudText Typo="Typo.caption">@product.ShortDescription</MudText>
                                </MudItem>
                                <MudItem xs="2">
                                    <MudChip T="string" Size="Size.Small" Color="Color.Info">
                                        @product.BrandName
                                    </MudChip>
                                </MudItem>
                                <MudItem xs="2">
                                    <MudText Typo="Typo.body2">@(product.DefaultPrice.HasValue ? product.DefaultPrice.Value.ToString("C2") : "-")</MudText>
                                </MudItem>
                                <MudItem xs="2">
                                    <MudChip T="string" Size="Size.Small" Color="@GetStatusColor(product.Status)">
                                        @product.Status
                                    </MudChip>
                                </MudItem>
                                <MudItem xs="2" Class="d-flex justify-end">
                                    <MudIconButton Icon="@Icons.Material.Filled.Edit" 
                                                   Size="Size.Small"
                                                   OnClick="@(() => EditProduct(product.Id))" />
                                    <MudIconButton Icon="@Icons.Material.Filled.Delete" 
                                                   Size="Size.Small"
                                                   Color="Color.Error"
                                                   OnClick="@(() => DeleteProduct(product))" />
                                </MudItem>
                            </MudGrid>
                        </MudPaper>
                    </ItemContent>
                </VirtualizedEFTable>
            }
            else if (!_isLoadingProducts)
            {
                <MudPaper Elevation="1" Class="pa-4">
                    <div class="text-center">
                        <MudIcon Icon="@Icons.Material.Outlined.Inventory2" Size="Size.Medium" Class="mb-4 mud-text-secondary" />
                        <MudText Typo="Typo.h6" Class="mb-2">
                            @(_products.Any() ?
                                                TranslationService.GetTranslation("product.noProductsMatchFilters", "Nessun prodotto corrisponde ai filtri applicati") :
                                                TranslationService.GetTranslation("product.noProductsFound", "Nessun prodotto trovato"))
                        </MudText>
                        @if (_products.Any())
                        {
                            <MudButton Variant="Variant.Text"
                                       Color="Color.Primary"
                                       StartIcon="@Icons.Material.Outlined.Clear"
                                       OnClick="@ClearFilters">
                                @TranslationService.GetTranslation("action.clearFilters", "Cancella filtri")
                            </MudButton>
                        }
                    </div>
                </MudPaper>
            }
            
            <!-- Pagination -->
            @if (_pagedResult != null)
            {
                <MudDivider Class="my-4" />
                <div class="d-flex justify-center align-center flex-wrap gap-4">
                    @if (_pagedResult.TotalPages > 1)
                    {
                        <MudPagination Count="@_pagedResult.TotalPages" 
                                       Selected="@_currentPage" 
                                       Color="Color.Primary"
                                       Size="Size.Medium"
                                       ShowFirstButton="true"
                                       ShowLastButton="true"
                                       SelectedChanged="@OnPageChanged" />
                    }
                    <MudText Typo="Typo.caption" Color="Color.Secondary">
                        @if (_pagedResult.TotalPages > 1)
                        {
                            @TranslationService.GetTranslation("pagination.info", 
                                "Pagina {0} di {1} - Totale: {2} prodotti", 
                                _currentPage, _pagedResult.TotalPages, _pagedResult.TotalCount)
                        }
                        else
                        {
                            @TranslationService.GetTranslation("pagination.singlePage", 
                                "Totale: {0} prodotti", 
                                _pagedResult.TotalCount)
                        }
                    </MudText>
                </div>
            }
        </div>
    </div>
}

@code {
    // Dialog options for fullscreen audit dialog
    private readonly DialogOptions _auditDialogOptions = new() 
    { 
        FullScreen = true, 
        CloseButton = true,
        MaxWidth = MaxWidth.False
    };
    
    // UI State Management
    private bool _isLoading = true;
    private bool _isLoadingProducts = false;

    // Filter and search state
    private string _searchTerm = string.Empty;
    private CancellationTokenSource? _searchDebounceCts;

    // Pagination state
    private PagedResult<ProductDto>? _pagedResult;
    private int _currentPage = 1;
    private int _pageSize = 20;

    // Data collections
    private List<ProductDto> _products = new();
    private HashSet<ProductDto> _selectedProducts = new();

    // Performance tracking
    private Stopwatch? _loadStopwatch;
    private long _initialMemoryUsage;

    // Dashboard configuration
    private List<DashboardMetric<ProductDto>> _dashboardMetrics = new()
    {
        new()
        {
            Title = "Totale Prodotti",
            Type = MetricType.Count,
            Icon = Icons.Material.Outlined.Inventory2,
            Color = "primary",
            Description = "Numero totale di prodotti",
            Format = "N0"
        },
        new()
        {
            Title = "Prodotti Attivi",
            Type = MetricType.Count,
            Filter = p => p.Status == ProductStatus.Active,
            Icon = Icons.Material.Outlined.CheckCircle,
            Color = "success",
            Description = "Prodotti attivi",
            Format = "N0"
        },
        new()
        {
            Title = "Con Immagini",
            Type = MetricType.Count,
            Filter = p => p.ImageDocumentId != null,
            Icon = Icons.Material.Outlined.Image,
            Color = "info",
            Description = "Prodotti con immagini",
            Format = "N0"
        },
        new()
        {
            Title = "Ultimi Aggiunti",
            Type = MetricType.Count,
            Filter = p => p.CreatedAt >= DateTime.Now.AddDays(-30),
            Icon = Icons.Material.Outlined.NewReleases,
            Color = "warning",
            Description = "Prodotti aggiunti negli ultimi 30 giorni",
            Format = "N0"
        }
    };

    private EventCallback<HashSet<ProductDto>> _selectedItemsChangedCallback => EventCallback.Factory.Create<HashSet<ProductDto>>(this, OnSelectedItemsChanged);

    /// <summary>
    /// Computed property for filtered products based on search criteria.
    /// </summary>
    private IEnumerable<ProductDto> _filteredProducts => 
        _products.Where(p => 
            // Search filter: check code, name, and description
            (string.IsNullOrEmpty(_searchTerm) || 
             p.Code.Contains(_searchTerm, StringComparison.OrdinalIgnoreCase) ||
             p.Name.Contains(_searchTerm, StringComparison.OrdinalIgnoreCase) ||
             (p.ShortDescription != null && p.ShortDescription.Contains(_searchTerm, StringComparison.OrdinalIgnoreCase))));

    /// <summary>
    /// Handles selection changes from EFTable
    /// </summary>
    private void OnSelectedItemsChanged(HashSet<ProductDto> items)
    {
        _selectedProducts = items;
        StateHasChanged();
    }

    /// <summary>
    /// Component initialization with performance tracking.
    /// </summary>
    protected override async Task OnInitializedAsync()
    {
        _loadStopwatch = Stopwatch.StartNew();
        _initialMemoryUsage = GC.GetTotalMemory(false);

        try
        {
            // Check authentication
            var isAuthenticated = await AuthService.IsAuthenticatedAsync();
            if (!isAuthenticated)
            {
                await ShowLoginDialogAsync();
                return;
            }

            // Load products
            await LoadProductsAsync();

            _loadStopwatch.Stop();
            Logger.LogInformation(
                "ProductManagement loaded {Count} products in {ElapsedMs}ms. Memory delta: {MemoryDelta}KB",
                _products?.Count ?? 0,
                _loadStopwatch.ElapsedMilliseconds,
                (GC.GetTotalMemory(false) - _initialMemoryUsage) / 1024
            );
        }
        catch (Exception ex)
        {
            Snackbar.Add(TranslationService.GetTranslation("product.loadingPageError", "Errore nel caricamento della pagina: {0}", ex.Message), Severity.Error);
            Logger.LogError(ex, "Error loading products page");
        }
        finally
        {
            _isLoading = false;
        }
    }

    /// <summary>
    /// Loads product data.
    /// </summary>
    private async Task LoadProductsAsync()
    {
        try
        {
            _isLoadingProducts = true;
            _pagedResult = await ProductService.GetProductsAsync(_currentPage, _pageSize);
            
            if (_pagedResult != null)
            {
                _products = _pagedResult.Items.ToList();
            }
        }
        catch (Exception ex)
        {
            Snackbar.Add(TranslationService.GetTranslation("product.loadingProductsError", "Errore nel caricamento dei prodotti: {0}", ex.Message), Severity.Error);
            Logger.LogError(ex, "Error loading products");
        }
        finally
        {
            _isLoadingProducts = false;
        }
    }

    /// <summary>
    /// Clears all active filters.
    /// </summary>
    private void ClearFilters()
    {
        _searchTerm = string.Empty;
        StateHasChanged();
    }

    private async Task OnSearchChanged()
    {
        _currentPage = 1; // Reset to page 1 when search changes
        
        // Cancel previous debounce if any
        _searchDebounceCts?.Cancel();
        _searchDebounceCts = new CancellationTokenSource();
        var token = _searchDebounceCts.Token;
        
        try
        {
            await Task.Delay(300, token); // Debounce with cancellation
            if (!token.IsCancellationRequested)
            {
                StateHasChanged();
            }
        }
        catch (OperationCanceledException)
        {
            // Swallow cancellation
        }
    }

    private async Task OnPageChanged(int page)
    {
        _currentPage = page;
        await LoadProductsAsync();
    }

    private void CreateProduct()
    {
        NavigationManager.NavigateTo("/product-management/products/new");
    }

    private void EditProduct(Guid id)
    {
        NavigationManager.NavigateTo($"/product-management/products/{id}");
    }

    private async Task DeleteProduct(ProductDto product)
    {
        var confirmTitle = TranslationService.GetTranslation("common.confirm", "Conferma");
        var confirmMessage = TranslationService.GetTranslation("product.confirmDelete", 
            "Sei sicuro di voler eliminare il prodotto '{0}'? Questa azione non può essere annullata.", 
            product.Name);

        var confirm = await DialogService.ShowMessageBox(
            confirmTitle,
            confirmMessage,
            yesText: TranslationService.GetTranslation("common.delete", "Elimina"),
            cancelText: TranslationService.GetTranslation("common.cancel", "Annulla"));

        if (confirm == true)
        {
            try
            {
                await ProductService.DeleteProductAsync(product.Id);
                _products.Remove(product);
                Snackbar.Add(TranslationService.GetTranslation("product.productDeleted", "Prodotto eliminato con successo!"), Severity.Success);
                StateHasChanged();
            }
            catch (Exception ex)
            {
                Snackbar.Add(TranslationService.GetTranslation("product.deleteProductError", 
                    "Errore nell'eliminazione del prodotto: {0}", ex.Message), Severity.Error);
                Logger.LogError(ex, "Error deleting product {ProductId}", product.Id);
            }
        }
    }

    /// <summary>
    /// Opens the audit log dialog for a product using IDialogService.
    /// </summary>
    private async Task ViewProductAuditLog(ProductDto product)
    {
        var parameters = new DialogParameters<AuditHistoryDialog>
        {
            { x => x.EntityType, "Product" },
            { x => x.EntityId, product.Id },
            { x => x.EntityName, product.Name }
        };

        await DialogService.ShowAsync<AuditHistoryDialog>(
            TranslationService.GetTranslation("audit.historyDialog.title", "Cronologia Modifiche"),
            parameters,
            _auditDialogOptions);
    }

    /// <summary>
    /// Deletes all selected products.
    /// </summary>
    private async Task DeleteSelectedProducts()
    {
        if (_selectedProducts.Count == 0)
            return;

        var confirmTitle = TranslationService.GetTranslation("common.confirm", "Conferma");
        var confirmMessage = TranslationService.GetTranslationFormatted("product.confirmDeleteMultiple", 
            "Sei sicuro di voler eliminare {0} prodotti selezionati? Questa azione non può essere annullata.", 
            _selectedProducts.Count);

        var confirm = await DialogService.ShowMessageBox(
            confirmTitle,
            confirmMessage,
            yesText: TranslationService.GetTranslation("common.delete", "Elimina"),
            cancelText: TranslationService.GetTranslation("common.cancel", "Annulla"));

        if (confirm == true)
        {
            try
            {
                var deletedCount = 0;
                var failedCount = 0;
                
                foreach (var product in _selectedProducts.ToList())
                {
                    try
                    {
                        await ProductService.DeleteProductAsync(product.Id);
                        _products.RemoveAll(p => p.Id == product.Id);
                        deletedCount++;
                    }
                    catch (Exception ex)
                    {
                        failedCount++;
                        Logger.LogError(ex, "Error deleting product {ProductId}", product.Id);
                    }
                }
                
                _selectedProducts.Clear();
                
                if (failedCount == 0)
                {
                    Snackbar.Add(TranslationService.GetTranslationFormatted("product.deletedMultiple", 
                        "{0} prodotti eliminati con successo!", deletedCount), Severity.Success);
                }
                else
                {
                    Snackbar.Add(TranslationService.GetTranslationFormatted("product.deletedMultiplePartial", 
                        "{0} prodotti eliminati, {1} falliti", deletedCount, failedCount), Severity.Warning);
                }
                
                StateHasChanged();
            }
            catch (Exception ex)
            {
                Snackbar.Add(TranslationService.GetTranslationFormatted("product.deleteError", 
                    "Errore nell'eliminazione dei prodotti: {0}", ex.Message), Severity.Error);
                Logger.LogError(ex, "Error deleting selected products");
            }
        }
    }

    private async Task ShowLoginDialogAsync()
    {
        var result = await AuthenticationDialogService.ShowLoginDialogAsync();
        if (result)
        {
            // Reload the page after successful login
            await OnInitializedAsync();
        }
    }

    /// <summary>
    /// Gets the color for the product status chip.
    /// </summary>
    private Color GetStatusColor(ProductStatus status)
    {
        return status switch
        {
            ProductStatus.Active => Color.Success,
            ProductStatus.Suspended => Color.Warning,
            ProductStatus.OutOfStock => Color.Error,
            ProductStatus.Deleted => Color.Error,
            _ => Color.Default
        };
    }
}
