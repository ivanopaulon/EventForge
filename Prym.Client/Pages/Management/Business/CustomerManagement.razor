@page "/business/customers"
@inject NavigationManager NavigationManager

<PageTitle>Clienti</PageTitle>

@code {
    protected override void OnInitialized()
    {
        // Redirect to unified BusinessPartyManagement with customers filter
        NavigationManager.NavigateTo("/business/parties/customers", replace: true);
    }
}
